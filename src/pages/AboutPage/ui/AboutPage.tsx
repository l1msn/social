import React, { JSX } from 'react';
import Page from '@/widgets/Page';

const AboutPage: React.FC = (): JSX.Element => {
    return <Page data-testid={'AboutPage'}></Page>;
};

export default AboutPage;
